# Raw responses, one long JSON line per request (see README, data/raw/): collapsed in GitHub's
# diff view and left out of its language statistics. Display only; git stores them as usual.
data/raw/**/*.ndjson linguist-generated=true
